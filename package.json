{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "setup": "yarn install && npx lerna bootstrap",
    "start:app": "cd packages/frontend && yarn start",
    "start:node": "cd packages/contracts && yarn start:node"
  }
}
