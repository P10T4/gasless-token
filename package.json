{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "setup": "yarn install && npx lerna bootstrap --npm-client=yarn",
    "deploy:local": "lerna run deploy:local",
    "start:node": "cd packages/contracts && yarn start"
  }
}
